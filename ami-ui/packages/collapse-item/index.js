import AmiCollapseItem from '../collapse/src/collapse-item.vue';

/* istanbul ignore next */
AmiCollapseItem.install = function(Vue) {
  Vue.component(AmiCollapseItem.name, AmiCollapseItem);
};

export default AmiCollapseItem;
